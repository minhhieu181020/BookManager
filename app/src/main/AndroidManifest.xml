<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lab2_mob204">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.AddTypeBookActivity"></activity>
        <activity android:name=".activity.AddBookActivity" />
        <activity android:name=".activity.AddUserActivity" />
        <activity android:name=".activity.ListUserActivity" />
        <activity android:name=".activity.AddBillActivity" />
        <activity android:name=".activity.BookActivity" />
        <activity android:name=".activity.TheloaiActivity" />
        <activity android:name=".activity.MenuActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>